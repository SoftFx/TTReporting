services:
  big-trades-local:
    image: big-trades
    environment:
      MT4_PASSWORD: ${MT4_PASSWORD}
      MT5_PASSWORD: ${MT5_PASSWORD}
      PG_PASSWORD: ${PG_PASSWORD}
    volumes:
      - ../../source/big-trades/configDocker:/app/configDocker:ro
      - ../../source/big-trades/dataDocker:/app/dataDocker:rw
    restart: "no"

