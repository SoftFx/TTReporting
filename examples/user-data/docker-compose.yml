services:
  user-data-local:
    image: user-data-app
    env_file: .env
    ports:
      - "3839:3838"
    volumes:
      - ../../source/user-data-App/configDocker:/app/configDocker:ro
    restart: unless-stopped
