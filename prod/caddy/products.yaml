# Products registry
# Adding a new product:
#   1. Add entry here
#   2. Add basicauth + service_proxy block in caddy/Caddyfile
#   3. Add service in docker-compose.yml with networks: [tt-internal]

- slug: aggr-data
  name: Aggregator Data
  description: |
    Interactive dashboard for LP execution analytics.
    Common statistics by liquidity providers: transactions, traded volume, slippage analysis.
  category: Aggregator
  # image: static/image/aggr-data.png
