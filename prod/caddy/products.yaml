# Products registry
# Adding a new product:
#   1. Add entry here
#   2. Add basic_auth + service_proxy block in caddy/Caddyfile
#   3. Add service in docker-compose.yml with networks: [tt-internal]

- slug: aggr-data
  name: Aggregator Data
  description: |
    Interactive dashboard for LP execution analytics.
    Common statistics by liquidity providers: transactions, traded volume, slippage analysis.
  category: Aggregator
  # image: static/image/aggr-data.png

- slug: user-data
  name: User Analytics
  description: |
    Trading account analytics (TT, MT4, MT5).
    Login-based summary: traded volume, swap markup, commission breakdown.
  category: Analytics
